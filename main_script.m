%   ASB TUTORIAL MASTER SCRIPT
%
%   Each of the scripts below has a change that needs to be made per
%   the reviewer's comments!
%

clear; close all;

% Make sure your MATLAB working directory is the repository folder!
addpath(strcat(pwd,'/MATLAB_scripts'))
addpath(strcat(pwd,'/data'))
figure()
hold on

script_1 % "U" patch

script_2 % "W" patch

script_3 % "M" patch

script_4 % banner shadow

script_5 % yellow banner
